
const UPDATE_TEST_DATA = "UPDATE-TEST-DATA";

const defaultState = {
 /*   stages:[

    ],
    questions:[

    ],
    answers:[

    ]*/
    value: 1

}


export const testDataReducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
