
const defaultState = {
    routeStudent: {
        status: 5
    }
}

export const routeReducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}